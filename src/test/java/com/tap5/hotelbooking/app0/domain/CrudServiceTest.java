package com.tap5.hotelbooking.app0.domain;

import com.tap5.hotelbooking.test.unit.BaseUnitTestSuite;

public class CrudServiceTest extends BaseUnitTestSuite
{
    /*
     * @SuppressWarnings("unchecked")
     * @Inject private CrudService crudService;
     * @Test public void create() { crudService = registry.getService(CrudService.class); User user
     * = new User(); user.setEmail("katiaaresti@gmail.com"); user.setFullname("Katia Aresti");
     * user.setPassword("12345"); user.setUsername("karesti"); crudService.create(user); assert
     * user.getId() != null; }
     */
}
